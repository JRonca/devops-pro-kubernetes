apiVersion: v1
kind: Pod
metadata:
  name: myapp
  labels:
    app.kubernetes.io/name: myapp
spec:
  containers:
  - name: myapp
    image: busybox
    command: # => Entrypoint
      - echo
    args: # => CMD
      - "Hello world"
  restartPolicy: Never
